node_modules/
*.vsix
*.post-pivot.bak
collision_report.json
config.ini
debug.log
assets/assets.zip
__pycache__/
*.pyc
.claude/

# faster-whisper STT runtime — venv (~150MB) + downloaded CT2 models + test PCM.
# Bootstrapped on first use by extension.js _ensureFasterWhisperVenv(); never track.
venv-whisper/
faster-whisper-models/
_fw_test.*
fw_install.log
panel/.index_tmp.pdf
panel/index.png
prompt_history.txt
prompts.txt
chats/

# Runtime data — browser-bridge Chromium profiles + bridge runtime state.
# Hold login cookies/sessions/tokens; NEVER track (hard rule). data/ is fully
# untracked; bridges/ has some legacy-tracked source files (see note) but new
# runtime/profile files under it must never be added.
data/
bridges/
emails/
logs/
skins/previews/
.skin_stubs/
skins/*.stub.html
*.db
*.sqlite
*.sqlite3
*.lastgood.db
*.real-backup
*.bak

# Lint / build / runtime droppings (never source)
*.pylintjson
*.log
port.txt
session.log
bin/*.exe
bin/*.manifest
bridges_cpp/obj/
bridges_cpp/*.res
bridges_cpp/*.rsp
tools/sign_metadata.json

# Scratch screenshots + skin-audit contact sheets + pre-edit asset backups
panel/_*.png
tools/_shot_*.png
tools/skin_snapshots/
_skin_audit_*/
assets/_pre_lineart_backup_*/
