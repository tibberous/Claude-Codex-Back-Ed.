Trace every code execution path. Is it inside a registered ApplicationLifeCycle phase? Does the phase have onFault / onError / onComplete / onException? Are running processes saved to a DB processes table with TTL + start_time? Does the main loop check TTL and task-kill stuck threads, marking them errored? Does start.py show the debug screen when a process row has status=errored, and print which process errored?
